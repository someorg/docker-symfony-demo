to run:

```
docker run -p 80:80 --name xpika_symfony_demo -i -t -d xpika/docker-symfony-demo
```

to build locally 
```
docker build -t xpika/symfony_demo .
```

Paths
* http://localhost/get_cms
* http://localhost/set_cms
